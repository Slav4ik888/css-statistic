import CredName from './actual-creds';
import checkCredential, { cred, noCred } from './check-credential';
import getUserRoleId from './get-user-role-id';
import whoseCred from './whose-cred';
import labelNoCred from './label-no-cred';


export {
  CredName,
  checkCredential, cred, noCred,
  getUserRoleId,
  whoseCred,
  labelNoCred
}
