/**
 * Get arr from arrs into arr by field
 *  Contact: {
 *   companies: [id1, id2 ...]
 *  }
 */
export function getArrFromArrsByFieldAndValue<T>(arr: Array<T>, fieldArr: string, value: string): Array<T>;
