import { strWithLength } from './str-with-length/index.js';
import { getMockStrLength } from './get-mock-str-length/index.js';
import { sliceStr, sliceStr30, sliceStr300, sliceStr1000 } from './slice-str/slice-str.js';

export {
  strWithLength, getMockStrLength,
  sliceStr, sliceStr30, sliceStr300, sliceStr1000
}
