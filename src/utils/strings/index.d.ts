import { strWithLength } from './str-with-length';
import { getMockStrLength } from './get-mock-str-length';
import { sliceStr, sliceStr30, sliceStr300, sliceStr1000 } from './slice-str/slice-str';

export {
  strWithLength, getMockStrLength,
  sliceStr, sliceStr30, sliceStr300, sliceStr1000
}
