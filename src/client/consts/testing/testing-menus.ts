import { TestingType } from "../../../types";

export default [
  {
    id        : TestingType.STATISTICS,
    label     : `Список DBs from LocalStorage`,
    toolLabel : `Список DBs from LocalStorage`
  }
]