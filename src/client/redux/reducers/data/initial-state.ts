import { StateData } from '../../redux-types';


export const initialState: StateData = {
  loading: false,
  selectedDates: null,

  cssDb: null,
  cssInstDb: null,
  cssExpDb: null,
  badcomDb: null
};