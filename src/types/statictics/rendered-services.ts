export enum ServiceType {
  INSTALL = `install`
};

export enum ServiceRange {
  KA = `Ka`,
  KU = `Ku`
};

export enum ServiceClient {
  LEGAL   = `ЮЛ`, // `Legal`,
  INDIVID = `ФЛ`  // `Individ`
};

export enum ServiceAge {
  NEW    =  `Новая`, // `New`,
  REPEAT =  `Повторная` // `Repeat`
};